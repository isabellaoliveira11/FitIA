import 'package:flutter/material.dart';

class AppColors {
  // Modo claro
  static const background = Color(0xFFF2FDF6);
  static const primary = Color(0xFF2E7D32);
  static const accent = Color(0xFF66BB6A);
  static const greenLight = Color(0xFFC8E6C9);
  static const textDark = Color(0xFF1B1B1B);
  static const textLight = Colors.grey;
  static const card = Colors.white;

  // Modo escuro
  static const darkBackground = Color(0xFF121212);
  static const darkCard = Color(0xFF1E1E1E);
  static const darkText = Colors.white;
  static const darkAccent = Color(0xFF81C784);
}
